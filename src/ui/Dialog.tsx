/** @deprecated Prefer `import { … } from "./kit/Dialog"` — kept for existing import paths. */
export {
  Dialog,
  DialogTrigger,
  DialogContent,
  DialogTitle,
  DialogDescription,
  DialogHeader,
} from "./kit/Dialog";
